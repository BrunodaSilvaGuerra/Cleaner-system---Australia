const saveJobSetupWithUiStatus=saveJobSetup;
saveJobSetup=async function(){const client=document.querySelector('#setupJobClient').value,cleaner=document.querySelector('#setupJobCleaner').value,date=document.querySelector('#setupJobDate').value,time=document.querySelector('#setupJobTime').value,duration=document.querySelector('#setupJobDuration').value,send=document.querySelector('#setupJobTelegram').value==='yes',cleanerIndex=team.findIndex(item=>item[1]===cleaner),profile=teamProfiles[cleanerIndex]||{};saveJobSetupWithUiStatus();if(!send)return;const row=document.querySelector('#jobsTable tr:last-child'),status=row&&row.lastElementChild.querySelector('.pill');if(status){status.className='pill waiting';status.textContent='Sending Telegram'}try{const response=await fetch('/api/telegram/send-job',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chatId:profile.telegramId,client,address:(clientLocationData[clients.findIndex(item=>item[1]===client)]||{}).address||'',date,time,duration})});const result=await response.json();if(!response.ok||!result.ok)throw new Error(result.error||'Unable to send Telegram message');if(status){status.className='pill sent';status.textContent='Telegram sent'}showOpsToast('Telegram request sent','The job was sent to '+cleaner+' for confirmation.')}catch(error){if(status){status.className='pill overdue';status.textContent='Telegram failed'}showOpsToast('Telegram was not sent',error.message)}};
