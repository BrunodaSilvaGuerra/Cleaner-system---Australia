bindClientSetupAddress = function(){const input=document.querySelector('#setupClientAddress'),results=document.querySelector('#setupAddressResults'),coordinates=document.querySelector('#setupCoordinates');input.addEventListener('input',()=>{clientSetupLocation=null;coordinates.textContent='No verified location selected';coordinates.classList.add('pending');results.innerHTML='';clearTimeout(clientSetupTimer);const query=input.value.trim();if(query.length<4)return;clientSetupTimer=setTimeout(async()=>{try{const url='https://nominatim.openstreetmap.org/search?format=jsonv2&addressdetails=1&countrycodes=au&limit=5&q='+encodeURIComponent(query);const response=await fetch(url,{headers:{Accept:'application/json'}});if(!response.ok)throw new Error();const places=await response.json();if(!places.length){results.innerHTML='<div class="address-result">No Australian address found. Add a suburb or postcode.</div>';return}places.forEach(place=>{const button=document.createElement('button');button.type='button';button.className='address-result';button.textContent=place.display_name;button.onclick=()=>{clientSetupLocation={lat:+place.lat,lon:+place.lon,address:place.display_name};input.value=place.display_name;results.innerHTML='';coordinates.classList.remove('pending');coordinates.textContent='✓ Verified coordinates: '+clientSetupLocation.lat.toFixed(6)+', '+clientSetupLocation.lon.toFixed(6)};results.append(button)})}catch{results.innerHTML='<div class="address-result">Address search is unavailable. Please try again.</div>'}},900)})}
